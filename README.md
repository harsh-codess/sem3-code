# Semester 3 Codebase

One codebase for all Sem 3 coding work.

- `c/refresher/` — C syntax refresher exercises (see checklist in `~/dsa-tracker/plan.md`)
- `c/dsa-lab/` — DS-I Lab experiments (C)
- `oop/` — OOPL exercises (C++)
- `dbms/` — DBMS SQL scripts/exercises

Study plan and tracker live separately in `~/dsa-tracker` (`plan.md`, `log.md`, `track`).
